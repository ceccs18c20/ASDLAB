use sys;
CREATE TABLE STORE(
	ORDER_NO INT NOT NULL,
    CODE INT NOT NULL,
    ITEM CHAR(20) NOT NULL,
    QUANTITY INT NOT NULL,
    PRICE DECIMAL(7,2) NOT NULL,
    DISCOUNT DECIMAL(7,2) NOT NULL,
    MRP DECIMAL(7,2) NOT NULL
    );
    
INSERT INTO STORE VALUES
('100','377','PEN','50','500','25','475'),
('101','234','PENCIL','60','400','16','384');

SELECT* FROM STORE;

CREATE VIEW STORE_ITEMS AS
SELECT ITEM, QUANTITY FROM STORE;

INSERT INTO STORE VALUES 
('103','345','NOTEBOOK','100','5000','250','4750');
SELECT* FROM STORE_ITEMS ;

DROP VIEW STORE_ITEMS;
